{"files": {"SKILL.md": "---\nname: anchor-verifier-fusion\ndescription: Compact fusion of the top-3 race agents. Orient fast, anchor at the exact path by action three, map the consumer pipeline, verify with the real harness, pivot mechanism after two same-class failures.\n---\n\n# Anchor-Verifier Fusion\n\n**Rule zero:** Always leave a structurally valid artifact at the required path. Partial correctness at the right path beats absence or structural breakage.\n\n**Rule one:** Declare success only after read-back and the real verifier or probe passes \u2014 not from intent or narration alone.\n\n## Operating loop\n\nEvery task follows the same loop. Do not skip stages.\n\n```\nORIENT \u2192 ANCHOR \u2192 PIPELINE \u2192 REFINE \u2192 CONFIRM\n   \u2191        |         |         |         |\n   \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518  (fix or pivot, then REFINE again)\n```\n\n| Stage | Budget | Goal | Stop when |\n|-------|--------|------|-----------|\n| **ORIENT** | \u2264 20% with ANCHOR | Know path, format, verifier, consumers | You can write a valid skeleton |\n| **ANCHOR** | (included above) | File exists at exact path, structurally valid | Written by action 3 at latest |\n| **PIPELINE** | during REFINE setup | Map accepts / rejects / mutates | You know the conservative intersection |\n| **REFINE** | \u2265 60% | Close verifier gaps one fix at a time | All checks pass or budget gone |\n| **CONFIRM** | last action | Read back + real verifier + live probe | Every exit criterion true |\n\n## ORIENT \u2014 parallel, then stop\n\nOne batch of independent reads:\n\n- Task brief and every file it names.\n- Existing deliverable path, if any.\n- Tests, validators, harness scripts, consumers of your output.\n- Nearby examples for naming, imports, formatting, error handling.\n\nExtract hard constraints:\n\n- Exact output path, filename, format, encoding, permissions.\n- Schema, headers, ordering, required wording.\n- Verification command or pipeline.\n- Forbidden surfaces.\n- Runtime expectations: services, reload, side effects, health checks.\n\nWhen the input is encoded or non-obvious, inspect raw bytes before trusting the application-level view. Trust exercised local code and failing assertions over memory or stale docs. Stop reading once you can anchor.\n\n## ANCHOR \u2014 write before deep investigation\n\nThe first write goes to the **exact required path**. Never a scratch file. Never prose-only planning. Never run the verifier before the anchor exists.\n\nMinimum anchor criteria:\n\n1. At the required path.\n2. Syntactically valid for the format.\n3. Passes structural checks (schema, header, required fields).\n\n| Format | Anchor |\n|--------|--------|\n| JSON object | `{}` or skeleton with required keys |\n| JSON array | `[]` or one-row skeleton |\n| CSV | header row only |\n| YAML / config | minimal valid skeleton |\n| Source | compilable stub with required declarations |\n| Script | shebang + harmless no-op |\n| Archive | smallest valid container |\n\nUse file tools for `$`, backticks, regex, quotes, or non-ASCII. Read the file back after every write.\n\n**Progress invariant:** after every edit, a structurally valid file must remain at the required path. Break structure \u2192 fix structure first, behavior second.\n\n## PIPELINE \u2014 design for the intersection\n\nIf output passes through parsers, validators, transport, storage, or a harness, sketch the chain:\n\n- **Accepts** at each stage\n- **Rejects** at each stage\n- **Mutates** (case, whitespace, line endings, key order, escaping, paths)\n\nDesign for the intersection. When unsure, probe the stage nearest the verifier, then walk backward.\n\nWhen output must satisfy an existing component, read that component's source before writing for it \u2014 intent and behavior differ; the gap is only visible in code.\n\nConfig edits: put directives in the scope that owns them. Prefer `conf.d` / `config.d` drop-ins over fragile in-place patches in large host configs.\n\n## REFINE \u2014 verifier-driven, one fix at a time\n\nAfter each intentional change:\n\n1. Re-read the file if written via shell or template.\n2. Run the **narrowest real check** first, then widen:\n   - syntax / schema / compile / one targeted test\n   - integration or consumer call\n   - full verifier or suite\n   - live probe: service up, endpoint reachable, side effect present\n3. Record the **first named failure** \u2014 that is the next fix.\n4. Make one cohesive change. Match existing code style. No drive-by refactors.\n\nRun the verifier the way it runs:\n\n- Pytest-shaped files need the pytest entry point \u2014 `python file.py` only imports them.\n- Shell harnesses need bash. Binaries need direct execution. Configs need the subsystem validator.\n- Exit code 0 with empty stdout is silence, not pass.\n\nFailure quick-map:\n\n| Signal | Likely fix |\n|--------|------------|\n| Missing path | Write to exact required path |\n| Parse / compile error | Restore structure before logic |\n| Unknown directive | Scope, nesting, typo, unsupported option |\n| Connection refused | Start service, bind, port, readiness |\n| Wrong count or value | Boundaries, ordering, case, indexing |\n| Missing records | Companion files, sidecars, dotfiles |\n| Looks right, still fails | Read the normalizing harness or consumer source |\n\n**Pivot rule:** two focused fixes in the same failure class \u2192 next attempt must change **mechanism** (library, IO path, drop-in, smaller repro, controlled rewrite). Never rerun the verifier without a deliberate change.\n\nFor numeric thresholds: run the scorer's measurement, change one variable, re-run. Three iterations with no movement \u2192 change mechanism.\n\n## Self-contained runnable artifacts\n\nAssume a fresh environment. Deliverables that execute later must install deps, create dirs, write configs, start services, and verify readiness themselves. Execute the script end-to-end in your environment before declaring done. Reset to the scorer's starting state for final validation. Cleanup on success, failure, timeout, and cancellation.\n\nAfter configuration changes: validate syntax \u2192 reload or start \u2192 probe what tests actually exercise.\n\n## Cross-episode memory\n\nWhen memory persists between episodes:\n\n- Read prior learnings before starting.\n- Append successes, failures, and patterns after finishing.\n- Timestamp facts; supersede stale entries on conflict.\n\nFormat: `[date] area | priority | observation | action`.\n\n## Partial delivery\n\nIf budget runs out:\n\n- Keep the best structurally valid content at the required path.\n- Do not delete correct partial work.\n- Do not swap computed partials for placeholders in a final pass.\n\n## Anti-patterns\n\n- All turns on investigation, zero on disk.\n- Scratch path + relocate at the end.\n- Anchor optional because the task looks easy.\n- Verifier before anchor.\n- Success declared without read-back or real check.\n- Config changed but service never reloaded.\n- Exploring after the answer is already visible in tool output.\n\n## CONFIRM \u2014 exit gate\n\nDone only when all are true:\n\n1. Deliverable at exact path, name, extension, format.\n2. Read back from disk.\n3. Real verifier / test / compile / probe run when available.\n4. Adjacent behavior not regressed.\n5. Live system state matches intent, not just edited files.\n\nStop exploring when the answer is in hand \u2014 the next action is the write or fix, not another probe.\n\nIf time is exhausted, leave the best valid partial artifact and report the remaining failure.\n"}, "schema_version": 1}