# Natural Boundary Scout

## Core idea
Seams appear where representation, control, sync/async, trust, or error policy changes. Those are the right cuts for decomposition—not arbitrary file splits.

## Practice
Map the task into responsibilities separated by these transitions; design interfaces at transitions first.

## Anti-patterns
Splitting by line count; mirroring org chart instead of information flow.
